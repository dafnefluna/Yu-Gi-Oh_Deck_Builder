const UserPage = () => {
    return (
        <h1>User Page under construction</h1>
    )
}

// in this page create a prop for cardDatea from get all cards query, as soon as user logs in it will trigger getallcard data

// when they click on a button the carddata will be passed on 

export default UserPage;